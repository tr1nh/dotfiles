#!/bin/bash

# Simple script to get hex color & save it to clipboard

hexcode=$(gpick -so --no-newline)
if [ -z $hexcode ]; then
	exit 0
else
	notify-send "Mã màu vừa lấy là:" $hexcode
	echo $hexcode | tr -d '\n' | xsel -i --clipboard
fi
